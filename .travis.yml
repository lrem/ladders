language: python
python:
  - "3.6"
before_install:
  - nvm install node
install:
  - pip install -r requirements.txt
  - cd web
  - nvm use node
  - npm install
  - cd ..
script:
  - mypy --ignore-missing-imports api.py
  - cd web
  - ng build
  - ng lint